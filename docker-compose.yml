version: "3"

services:
  deepseek2api:
    container_name: deepseek2api
    build:
      context: .
    image: deerfishsheep/deepseek2api:local
    restart: always
    ports:
      - "5001:5001"
    volumes:
      - "./config.json:/app/config.json"
